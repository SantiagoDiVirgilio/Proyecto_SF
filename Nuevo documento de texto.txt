/*-----------------------------------------------VERSIÓN DE MÓBILES----------------------------------------------*/
@media screen and (max-width: 480px) {
  /* Ajustes generales del body */
  body {
    display: flex;
    flex-direction: column;
  }

  /* Header y Navegación */
  header {
    flex-direction: column; /* Revertido a column */
    align-items: center;
    padding: 10px;
  }

  .mobile-header-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%; /* Asegura que ocupe todo el ancho disponible */
  }

  .logo {
    width: 40%; /* Ajusta el tamaño del logo para móvil */
    margin-bottom: 0;
  }

  nav {
    width: 100%;
    justify-content: center;
  }

  nav > ul {
    flex-direction: column;
    padding: 0;
    width: 100%;
  }

  nav ul li {
    padding: 0.5rem 0;
    text-align: center;
    width: 100%;
    border-bottom: 1px solid #444;
  }

  nav ul li:last-child {
    border-bottom: none;
  }

  /* Contenedores principales y columnas */
  .container-principal,
  .info_per,
  .info_per:nth-child(even) {
    flex-direction: column;
  }

  .columna-fondo,
  .columna-texto,
  .columna-texto-2,
  .formu_side {
    display: none; /* Ocultar fondos de columna en móvil para simplicidad */
  }

  .columna-texto,
  .columna-texto-2 {
    display: block;
  }

  /* Personajes */
  .info_per {
    margin-bottom: 2rem;
  }

  .icon_per {
    width: 100%;
    height: auto;
    border-radius: 15px 15px 0 0;
  }

  .info_per p {
    padding: 1.5rem;
  }
  /* Galería */
  .gallery-section > div {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-around;
  }
  .gallery-section .zoom {
    flex-basis: 45%;
    margin-bottom: 1rem;
  }
  .gallery-section .lightbox-min,
  .gallery-section .lightbox-min-2 {
    width: 100%;
  }
 .gallery-section .lightbox-min {
    margin-top: 1rem;
  }

  /* Formulario */
  .formu {
    flex: 1;
    width: 90%;
    margin: 1rem auto;
    height: auto;
    color: #ffffff;
  }

  .formu form input[type="text"],
  .formu form input[type="password"],
  .formu form input[type="email"] {
    width: 90%;
  }

  /* Footer */
  footer {
    padding: 2rem 1rem;
    text-align: center; /* Centra los hijos en línea (imágenes) y los bloques */
  }

  footer div {
    margin: 1rem auto 0; /* Espacio reducido entre los grupos de enlaces */
    display: flex;
    flex-direction: column;
    align-items: center; /* Centra el contenido de cada grupo */
  }

  /* Oculta los <br> para un control de espaciado más preciso con CSS */
  footer div br {
    display: none;
  }

  footer div h7 {
    display: block;
    margin: 0 0 0.75rem 0; /* Centrado y con margen inferior */
    font-size: 1.1rem;
  }

  footer div a {
    display: block;
    margin: 0.25rem 0 0 0; /* Centrado y con margen superior reducido */
    font-size: 1.1rem;
  }

  /* Elimina el margen superior del primer enlace para que quede más pegado al título */
  footer div h7 + a {
    margin-top: 0;
  }

  /* Contenedores de los íconos de redes sociales */
  footer > a {
    display: inline-block;
    margin-top: 1.5rem; /* Baja los íconos para separarlos de los enlaces */
  }

  /* Estilos de los íconos de redes sociales */
  footer img.redes {
    width: 35px;
    height: 35px;
    margin: 0.5rem; /* Espacio alrededor de cada ícono */
    vertical-align: middle; /* Alinea mejor los íconos en la línea */
  }
  
  /* Ocultar menú de escritorio y mostrar ícono de hamburguesa */
  nav > ul {
    display: none;
  }

  .icon {
    display: block;
    color: white;
    font-size: 24px;
    cursor: pointer;
  }

  /* Estilos para el menú desplegable en móvil */
  nav.responsive > ul {
    display: flex;
    flex-direction: column;
    width: 100%;
    background-color: #270e03;
  }
}